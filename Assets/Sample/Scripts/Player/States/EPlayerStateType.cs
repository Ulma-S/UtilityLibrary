namespace RitsGameSeminar.Sample {
    /// <summary>
    /// Stateの種類のenum.
    /// (1行目は空のtypeを設定すること)
    /// </summary>
    public enum EPlayerStateType {
        None,
        Idle,
        Run,
        Jump,
    }
}